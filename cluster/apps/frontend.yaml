apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: project-frontend
  namespace: flux-system
spec:
  sourceRef:
    kind: GitRepository
    name: dwk-repo
  interval: 10m
  path: './cluster/apps/todo-app-frontend/manifests'
  prune: true
  validation: client